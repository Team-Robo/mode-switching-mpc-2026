# Deployment-only dependencies (Intel i3 CPU)
# pip install -r requirements_deploy.txt

# CPU-only PyTorch (much smaller install, ~200MB vs ~2GB)
torch==2.0.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Only need SB3 for model loading (predict), not training
stable-baselines3==2.1.0
gymnasium==0.29.1

# Optional: ONNX Runtime for even faster inference (~0.05ms vs ~0.3ms)
# onnxruntime==1.16.0

numpy>=1.24,<2.0
rospkg
